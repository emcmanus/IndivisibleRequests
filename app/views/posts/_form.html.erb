<%= simple_form_for(@post, html: { class: 'form-vertical' }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.association :user %>
  <%= f.input :view_count %>
  <%= f.button :submit %>
<% end %>
